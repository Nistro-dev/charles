import { User } from './User';

// Export all models
export { User };

// Initialize associations here if needed
export function initializeModels() {
  // Add associations here when you have multiple models
  // Example: User.hasMany(Post);
} 